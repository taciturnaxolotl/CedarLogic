// Generated by cmake/module/EmbedResources.cmake. Do not edit.

#include "EmbeddedRes.h"

#include <cstring>

namespace {

@blobs@struct Entry {
	const char* name;
	const unsigned char* data;
	std::size_t size;
};

const Entry kEntries[] = {
@entries@};

}  // namespace

namespace cl {
namespace res {

Blob find(const char* name) {
	if (name) {
		for (const Entry& e : kEntries) {
			if (std::strcmp(e.name, name) == 0) return Blob{e.data, e.size};
		}
	}
	return Blob{nullptr, 0};
}

std::string text(const char* name) {
	Blob b = find(name);
	return b.ok() ? std::string(reinterpret_cast<const char*>(b.data), b.size)
	              : std::string();
}

}  // namespace res
}  // namespace cl
