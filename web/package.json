{
  "name": "@cedarville/cedarlogic-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CedarLogic in the browser: the desktop core compiled to wasm, drawn with Canvas2D",
  "scripts": {
    "engine": "../wasm/gui/build.sh",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
